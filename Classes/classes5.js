class Room{
  constructor(roomId, level, amount, isFree){
    this.roomId = roomId;
    this.level = level;
    this.amount = amount;
    this.isFree = isFree;
    
    isFree = false;
  }

  numPeople(amount){
    if()
  }
}